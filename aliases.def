# Shared shortcuts — one per line: name=command
# Both .bashrc (alias) and config.fish (abbr) read this file directly on startup.

la=ls -A
ll=ls -lh
lla=ls -la
ga=git add .
gb=git branch
gd=git diff
gl=git log --oneline
gp=git push
gs=git status
nv=nvim
